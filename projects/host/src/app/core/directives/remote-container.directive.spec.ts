import { RemoteContainerDirective } from './remote-container.directive';

describe('RemoteContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoteContainerDirective();
    expect(directive).toBeTruthy();
  });
});
